form {
    .form-group {
        @apply mb-4;
    }
    label {
        @apply block;
    }
    input[type="text"], input[type="search"]{
        @apply bg-gray text-white px-6 py-3 rounded outline-none;
    }
}