button, .button{
    @apply flex gap-2 items-center bg-purple rounded px-6 py-3 text-black cursor-pointer transition duration-250 ease-in-out;

    &:hover {
        @apply bg-cyan;
    }
    img { 
        @apply h-6;
    }
    span {
        @apply block;
    }
}